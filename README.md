# Daily Task Tracker

A simple **Python command-line application** for tracking daily tasks and reminders.
Tasks and reminders are stored using **JSON files** for persistence.

## Tech Used

* Python
* JSON for data storage

## Future Plans

The project will later include a **frontend interface** that interacts with the program through an **API**, allowing tasks and reminders to be managed through a graphical interface instead of the command line.
